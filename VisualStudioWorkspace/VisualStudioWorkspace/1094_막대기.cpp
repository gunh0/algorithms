#include <iostream>

using namespace std;

int main()
{
	int input = 0;
	cin >> input;
	int stick = 64, sum=0, cnt=0;
	while (input > 0)
	{
		if(stick > input)
	}
	return 0;
}
/*
		Scanner sc = new Scanner(System.in);
		int length = 64;
		int x = sc.nextInt();
		int count = 0;
		int sum = 0;

		while (x > 0) {

			if (length > x) {
				length /= 2;
			}
			else {
				count++;
				x -= length;
			}
		}
		System.out.println(count);
 */